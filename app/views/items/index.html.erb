<h1>List of all items</h1>

<ul>
  <% @items.each do |item| %>
    <li>
      <%= item.item_name %>
      <%= item.item_name %>
      <%= item.item_name %>
    </li>
  <% end %>
</ul>

<%= link_to 'Add Item', new_item_path %>